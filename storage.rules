rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /cards/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
    match /memories/{allPaths=**} {
      // Read allowed with download URL token (URLs only served to authenticated users via Firestore)
      allow read: if true;
      allow write: if request.auth != null
                   && request.auth.token.admin == true;
    }
  }
}
