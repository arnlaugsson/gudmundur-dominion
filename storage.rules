rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Card images are public read
    match /cards/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
